import 'package:flutter/material.dart';
import '../widgets/add start current balance/add_start_current_balance_body.dart';

class AddStartCurrentBalance extends StatelessWidget {
  const AddStartCurrentBalance({super.key});

  @override
  Widget build(BuildContext context) {
    return AddStartCurrentBalanceBody();
  }
}
